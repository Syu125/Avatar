<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.avatar">

    <application
        android:allowBackup="true"
        android:icon="@drawable/app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DisplayElement"></activity>
        <activity android:name=".DisplayElementA"></activity>

        <activity android:name=".DisplayElementW"></activity>
        <activity android:name=".DisplayElementE"></activity>

        <activity android:name=".profile" />
        <activity android:name=".WelcomeScreen" />
        <activity android:name=".Menu" />
        <activity android:name=".today" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>